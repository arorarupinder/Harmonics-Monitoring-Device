# peripheral writes data

from easy_comms import Easy_comms
from time import sleep
from machine import Pin
import json

com1 = Easy_comms(0,9600)
led = Pin(25, Pin.OUT)
data = [1,2,3,4,5]
while True:
    com1.send(str(json.dumps(data)))
    led.toggle()
    sleep(1)
